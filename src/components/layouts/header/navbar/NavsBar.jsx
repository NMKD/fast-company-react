import React from "react";
import Links from "./links";

const NavsBar = () => {
    return (
        <>
            <ul className="nav">
                <Links />
            </ul>
        </>
    );
};

export default NavsBar;
